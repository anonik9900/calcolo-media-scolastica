English --

Use main.cpp file to compile if you don't have Codeblock or use CODEBLOCK_project if you have.

Italiano -- Italian

Usa il file main.cpp per compilare il programma se non hai codeblock , se invece hai codeblock usa il progetto nella cartella Only_CODEBLOCKS.